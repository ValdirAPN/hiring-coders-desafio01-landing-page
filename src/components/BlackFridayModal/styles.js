import styled from "styled-components";

export const Container = styled.div`
  text-align: center;
  text-transform: uppercase;

  h1 {
    font-size: 3rem;
    line-height: 2.5rem;
    margin-bottom: 3rem;
  }

  span {
    color: var(--gold);
  }
`;